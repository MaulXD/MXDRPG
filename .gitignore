# Build artifacts (never commit)
.next/
node_modules/
out/
.vercel/
*.tsbuildinfo
next-env.d.ts

# Runtime user registry (seed stays in git)
data/users/registry.json
data/characters/registry.json
data/characters/um-anel-registry.json
data/bug-reports/*.json

# Legacy web/ (app na raiz)
web/node_modules/
web/.next/

# Secrets
.env
.env.*
!.env.example
!.env.homolog.example
**/.env*.local

# OS / IDE / Python
.DS_Store
Thumbs.db
*.log
.cursor/
__pycache__/
*.pyc

public/uploads/

# Previews locais de design (não versionar)
preview-ficha.html

# Livros originais comprados pelo usuário (PDFs comerciais) — nunca versionar,
# é redistribuição de material com direitos autorais. O conteúdo extraído/
# transcrito em livros/um-anel/*.md é versionado à parte, por decisão do usuário.
/the one ring/

# Rascunho das traduções do Um Anel — o tradutor escreve aqui e só é movido
# para livros/ depois de passar na verificação de completude. Nunca versionar.
.tmp-traducao/
