import { EldarinCorners } from "@/components/ui/EldarinCorners";

/** Cantos ornamentais da ficha (estilo Foundry / pergaminho). */
export function SheetPopupOrnaments() {
  return <EldarinCorners className="sheet-popup-oc" />;
}
